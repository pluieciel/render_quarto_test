---
title: "Quarto Basics"
format: pdf
---

Best dataset ever:

```{r}
library(michelRodange)

data(renert)

names(renert) 

```

But this one is not bad either:

```{r}
head(mtcars)
```

```{r}
sessionInfo()
```
